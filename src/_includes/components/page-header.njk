{% macro pageHeader(kicker, title, intro) %}
<header class="page-header shell">
  <p class="eyebrow">{{ kicker }}</p>
  <h1>{{ title }}</h1>
  {% if intro %}<p class="page-header__intro">{{ intro }}</p>{% endif %}
</header>
{% endmacro %}
